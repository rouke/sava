name: sava_monolith_1_0

endpoints:
  sava.ports.port: 80

clusters:

  sava:
    services:
      breed:
        name: sava_monolith_1_0
        deployable: magneticio/sava-1.0_monolith:latest
        ports:
          name: port
          value: 80/http
          direction: OUT
